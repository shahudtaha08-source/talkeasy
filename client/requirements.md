## Packages
date-fns | Formatting dates and times beautifully
recharts | Rendering statistical charts for mood and habits
framer-motion | Page transitions and subtle animations

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
Requires Replit Auth to be active for /api/login and /api/auth/user endpoints.
Chatbot uses SSE at /api/conversations/:id/messages.
